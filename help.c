void help()
{
    vprint("Available Commands\n", 0x04);
    vprint("help - shows this dialouge\n", 0x02);
    vprint("lsd - prints crazy colors\n", 0x02);
    vprint("info - shows info about this operating system\n", 0x02);
    vprint("changelog - shows changelog of V-OS\n", 0x02);
    vprint("reboot - reboots (or sometimes crashes) system\n", 0x02);
    vprint("latin - switches v-os to latin language mode (WIP)", 0x02);
}
